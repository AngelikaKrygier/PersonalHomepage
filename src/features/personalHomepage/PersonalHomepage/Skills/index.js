import { Section } from "../Section";
import { skillsList } from "./skillsList";

export const Skills = () => (
    <Section
        tittle="My skillset includes 🛠️"
        skills={skillsList}
    >
    </Section>
);